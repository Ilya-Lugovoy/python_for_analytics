import random
import math
# list_res = []
# for i in range(9):
#     list_res.append(random.randint(1, 10)) # Создаёт случайное число в выбранном диапазоне
# print(list_res)

# list_of_orbits = [(1, 3), (6, 6), (7, 2), (2.5, 10), (10, 1)]
# areas = [(math.pi * a * b if a != b else 0) for a, b in list_of_orbits]
# max_area_index = areas.index(max(areas)) # max() - находит максимальное значение из коллекции; col.index() - определяет индекс элемента коллекции
# print(list_of_orbits[max_area_index])